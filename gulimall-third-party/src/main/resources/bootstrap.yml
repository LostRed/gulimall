spring:
  application:
    name: gulimall-third-party
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: third-party
        group: dev
        file-extension: yml
        extension-configs:
          - data-id: oss.yml
            group: dev
            refresh: false
